services:
  app:
    image: wasinth/simple-commerce-backend:latest
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - static_volume:/app/staticfiles
    restart: unless-stopped  # restart the container unless it is stopped

volumes:
  static_volume:
